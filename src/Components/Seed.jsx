
import React from 'react'

function Seed({ seed, index, imageUrl, name, type }) {
  return (
    <div
      style={{
        backgroundColor: 'black',
      }}
    >
      {type}
    </div>
  )
}

export default Seed